{
  "name": "v1-resonance",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "zip": "npm run build && (cd dist && zip -r ../v1-resonance.zip .)"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
